# 2026-08-27

## 为 Pyscript 项目安装 skills
- 从 `/Users/liuyongchao/Documents/GitHub/LINK.Core/.codebuddy/skills/` 完整复制了 26 个 skills（48 个文件，含各 skill 的 SKILL.md 和 references/ 子目录）到当前项目 `/Users/liuyongchao/NutstoreFiles/MyFiles/TotalLINK/Pyscript/.codebuddy/skills/`。
- 这些是 Matt Pocock 的通用 engineering skills 集合（code-review、diagnosing-bugs、tdd、implement、wayfinder、triage、to-spec、prototype、research 等），跨项目通用，不绑定特定语言。
- 注意：`setup-matt-pocock-skills` 是引导 skill，首次使用其他 skills 前需运行一次以配置该仓库的 issue tracker、triage labels 和 domain docs 布局（会生成 docs/agents/*.md 并更新 AGENTS.md/CLAUDE.md）。

## 运行 setup-matt-pocock-skills 完成仓库配置
- 配置决定：issue tracker = **GitHub**（gh CLI）；triage labels = **默认五个**（needs-triage/needs-info/ready-for-agent/ready-for-human/wontfix）；domain docs = **single-context**。
- 创建的文件：`AGENTS.md`（含 Agent skills block）、`docs/agents/issue-tracker.md`、`docs/agents/triage-labels.md`、`docs/agents/domain.md`。
- 后续如需切换 issue tracker 或修改 labels，直接编辑 `docs/agents/*.md` 即可，无需重跑 setup。

## git: 取消 .DS_Store 追踪并推送
- 用 `git rm --cached` 取消 `BROTEX/.DS_Store` 和 `mcp-server/.DS_Store` 追踪（本地保留），`.gitignore` 已含 `.DS_Store`。
- 提交 `d524b1a`（chore）包含：skills、AGENTS.md、docs/agents、Tamper/reference 及 .DS_Store 删除，已推送到 GitHub main 分支。

## Tamper：实现销售订单新增接口（saleOrder/Create）LinkPython 程序
通过 grill-with-docs + domain-modeling 打磨设计后落地，方案见 `docs/adr/0001/0002/0003`。
- **新增文件（Tamper/）**：`config.py`（token 占位，后续认证任务写入）、`chanjet_client.py`（共享 HTTP/认证，urllib 零依赖）、`sale_order.py`（linkrun 入口）、三个 TotalLINK XML 模型（`TSALESSOAPIM10` 的 700 输入/501 执行/701 结果）。
- **领域模型**：新建根 `CONTEXT-MAP.md`（BROTEX/Tamper/mcp-server 三 context）、`Tamper/CONTEXT.md` 词汇表、`docs/adr/0001-0003`。
- **关键设计决策**：①每接口一文件+共享 chanjet_client；②LinkPython 自包含（转换+HTTP+内部认证），返回契约 `isSuccess/message + results[]`；③输入字段名与用友一致但结构扁平（`CustomerCode→dto.Customer.Code`、`children→SaleOrderDetails`、`InventoryCode→Inventory.Code`、`UnitName→Unit.Name`），LinkPython 负责结构重组；④同步调用；⑤不启用 isautoaudit；⑥多订单数组逐单推送。
- **待办**：openToken 认证自动获取/刷新另开任务；XML 模型里订单来源表/落库表（TSALESORDER 等）为占位，需按 TotalLINK 实际表结构调整；需在配置真实 openToken 后联调。
- 销售订单接口已提交推送，commit `a1a035c`。

## 下一会话：采购订单接口（purchaseOrder/Create）
- 用户将新开会话处理采购订单接口，已生成 handoff 文档（临时目录 `tamper-purchase-order-handoff.md`）。
- 要点：照抄销售订单模式；需向用户索取 `purchaseOrder/Create` 确切请求体（勿凭空猜字段）；确认付款条款 PayType 映射、供应商编码来源；新建 `purchase_order.py` 与对应 XML；建议用 grill-with-docs 定稿。
